import ProjectDescription

// Tuist 4.x root config (replaces the legacy `Tuist/Config.swift`).
// Keep minimal; expand generationOptions only if needed.
let tuist = Tuist(
    project: .tuist(
        generationOptions: .options()
    )
)
